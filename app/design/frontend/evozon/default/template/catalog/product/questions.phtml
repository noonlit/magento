<?php $questions = $this->fetchQuestions();
?>
<div class="reviews-container">
    <?php foreach ($questions as $question): ?>
        <div class="row-question">
            <p><?= $question->getData('email');?></p>
            <p><?= $question->getData('text');?></p>
            <p><?= $question->getData('answer');?></p>
        </div>
    <?php endforeach; ?>
</div>
